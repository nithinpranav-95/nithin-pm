# Publish latest edits to the live site

## Why
Your edits to `src/routes/index.tsx` (e.g. the "Youtube" project title) are saved and already visible in the preview. The published site at `nithinforpm.lovable.app` is behind because it only updates on publish.

## Steps
1. Publish the project so the live site matches the preview.
2. Confirm the "Youtube" project title (and any other edits) appear on the published URL.
